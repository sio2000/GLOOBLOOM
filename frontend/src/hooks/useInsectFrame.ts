/** Insect / creature motion only — runs while the plant stays static on mobile. */
export { useCreatureFrame as useInsectFrame } from "@/hooks/useCreatureFrame";
export type { CreatureFrameContext as InsectFrameContext } from "@/hooks/useCreatureFrame";
